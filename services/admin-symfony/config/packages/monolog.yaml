monolog:
  handlers:
    main:
      type: stream
      # stderr, not a file: Cloud Run and App Engine collect the stream, and a
      # file inside an ephemeral container is written to be thrown away.
      path: php://stderr
      level: info
      channels: ['!event']
